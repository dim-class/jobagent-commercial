# 安装与试用（Windows）

给第一次拿到这个项目的人。**它在你自己的电脑上运行**：后端只监听
`127.0.0.1`，数据存在本地 SQLite，没有服务器、没有账号、没有任何数据离开这台机器。

> **先读这一条。** 这个工具会用**你自己的 BOSS 直聘账号**在真实网站上操作：
> 打开岗位详情、（在你逐个确认后）点击「立即沟通」并发送招呼语。频繁操作**可能
> 触发 BOSS 的风控**——本项目的开发账号就被限制过一次。你要为自己账号的后果负责。
> 投递功能默认是**关闭**的（见第 6 节），建议先只用搜索和分析。

---

## 1. 需要准备

| | |
| --- | --- |
| 操作系统 | Windows 10/11（只在 Windows 上验证过） |
| Python | 3.11 以上 |
| Node.js | 20 以上 |
| 浏览器 | 普通的 Chrome，**已经登录 BOSS 直聘** |
| OpenAI API Key | 可选。搜索和采集不需要；AI 分析需要 |

## 2. 装好并跑起来

在项目根目录打开 PowerShell：

```powershell
.\scripts\dev.ps1 setup    # 建虚拟环境、装依赖、生成 .env
.\scripts\dev.ps1 seed     # 建数据库
.\scripts\dev.ps1 start    # 后端 :8000、前端 :5173，各开一个窗口
```

浏览器打开 <http://127.0.0.1:5173>。看到左边有「搜索岗位 / 投递队列 / 岗位库 /
简历」就说明起来了。

## 3. 装扩展（必须，且要记住怎么重装）

搜索靠一个 Chrome 扩展在你已登录的页面上读取岗位，**它不会读 cookie、密码或任何
登录信息**。

1. Chrome 打开 `chrome://extensions`
2. 右上角打开「开发者模式」
3. 点「加载已解压的扩展程序」，选择项目里的 `extension` 文件夹

> **以后每次更新代码，都要回到这个页面点一次「重新加载」。**
> 磁盘上的新代码不会自动进入 Chrome——这是最容易让人以为"按钮没反应"的原因。

## 4. 填三样东西

控制台首页会直接告诉你还差什么。三样都在页面上：

1. **简历** —— 「简历」页上传 PDF/DOCX，设为「当前分析简历」
2. **岗位方向** —— 「更多工具 → 个人设置」填你想搜的岗位名（中文命中率远高于英文）
3. **AI 接口**（要用 AI 分析才需要）—— 「更多工具 → 设置 → AI 接口」，
   填你自己的 API Key，**保存即生效，不用重启**。

   支持任何 **OpenAI 兼容**的接口：接口地址留空就是 OpenAI 官方；用 DeepSeek、
   Moonshot、通义等，把各自的接口地址和模型名填进去即可。Key 只写进后端的
   `backend\.env`，不进数据库、不进日志、任何接口都不会把它返回给前端。

## 5. 跑第一轮

1. 在普通 Chrome 里打开并登录 BOSS 直聘，**这个标签页别关**
2. 回到控制台「搜索岗位」，选城市、填每个方向要收集多少个新岗位
3. 展开「搜索选项」：勾上「后台搜索」你就能同时干别的；不勾的话，切走窗口搜索会立刻停
4. 点「开始搜索」

一轮 16 个方向大约 30 分钟。跑完页面会提示「N 个岗位还没做 AI 分析」，点过去批量
分析（**要花钱**，确认前会显示确切的调用次数）。

## 6. 投递功能（已开启）

打包版里，「立即沟通」的投递功能是**开着**的。请先看清楚它的边界：

- **每一个岗位都要你单独确认一次**。确认框会显示这个岗位、公司、网址和你选的简历，
  你点了才会执行，一次确认只授权一次投递。
- **没有批量投递，没有定时，没有自动重试**。失败了不会重来，要投就再确认一次。
- 它会点「立即沟通」，并在聊天框为空时发送你在确认框里看到、可以改的那条招呼语。
  **之后的每一条消息都不会发**——后续跟进是你自己的事。
- 登录、验证码、风控、页面对不上，任何一种都会立刻停下并把控制权还给你。

不想要就关掉：在 `data\.env` 里写 `HUMAN_CONFIRMED_APPLY_ENABLED=false`，重启即可。

> 再说一次第 0 节那句：这是**你自己的 BOSS 账号**在真实发消息。建议先只用搜索和
> 分析跑几天，确认没有异常再开始投。

## 7. 出问题先看这里

| 症状 | 多半是 |
| --- | --- |
| 点「开始搜索」没反应 | 扩展没重新加载（第 3 节），或后端改动后没重启 |
| 页面报「无法连接后端服务」 | 后端窗口关掉了，`.\scripts\dev.ps1 start` 重开 |
| 提示「扩展是旧版本」 | 就是字面意思，去 `chrome://extensions` 点重新加载 |
| 搜索一开始就停 | 「搜索选项」里没勾「后台搜索」，而你切走了窗口 |
| 岗位没有薪资 | 正常。BOSS 列表里薪资是特殊字体，读不到；搜索结束后会自动补全 |
| BOSS 要求登录/出现验证码 | 搜索会立即停止，不会绕过。你自己在页面上处理完再点「恢复」 |

## 8. 它不会做的事

不批量投递、不自动投递、不定时运行、不在后台偷偷开标签页、不读你的 cookie 或密码、
不绕验证码、不代你回复 HR。这些不是"暂未实现"，是设计上就禁止的，写在
`CLAUDE.md` 里。
