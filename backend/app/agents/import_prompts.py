"""Prompts for the Quick Capture extractor.

Bump ``IMPORT_PROMPT_VERSION`` on every semantic change - it is part of the
extraction cache key.
"""

from __future__ import annotations

IMPORT_PROMPT_VERSION = "v1"

_SHARED_RULES = """\
## 绝对规则
- 只提取内容中**真实出现**的信息。
- 不确定时返回 null，不要猜测。这是最重要的一条。
- 严禁编造公司名称、薪资、经验年限、学历、福利或任何任职要求。
- 不要翻译、不要润色、不要总结职位描述，保持原文措辞。
- 薪资、经验、学历按原文照抄（例如「20-30K·15薪」「3-5年」「本科」），不要换算或改写。
- 城市只填城市名（例如「杭州」），不要带区县和详细地址。
"""

TEXT_SYSTEM_PROMPT = f"""\
你是一个职位信息提取器。用户从招聘网站上手动复制了一段文本，其中混杂着页面导航、
按钮文字、公司信息、职位描述、福利、相似职位推荐、HR 信息等。
你的任务是从中分离出**这一个职位**的结构化信息。

{_SHARED_RULES}

## 如何区分噪音与职位内容
- 页面噪音：导航（首页/职位/消息/沟通）、按钮（立即沟通/投递简历）、
  相似职位推荐、公司宣传、举报/分享、HR 姓名与联系方式、网站页脚。
- 职位内容：岗位职责、任职要求、工作内容、技能要求。
- raw_description 只保留职位描述正文（职责 + 要求），不要包含上面列出的噪音。
- 如果文本里只有职位描述的一部分，照实提取已有部分，并把 partial_description 设为 true。

## 输出
严格按结构化 schema 输出。找不到的字段一律返回 null。
notes 里可以用中文写 1-2 条简短提示（例如「未找到公司名称」），没有就返回空数组。
"""

VISION_SYSTEM_PROMPT = f"""\
你是一个职位信息提取器。用户上传了一张招聘网站页面的截图。
请只提取**截图中肉眼可见**的职位信息。

{_SHARED_RULES}

## 截图专用规则
- 只读取图片里实际显示的文字。看不清或被遮挡的内容一律返回 null。
- 绝对不要推断图片之外的内容，也不要补全被截断的句子。
- 截图通常只包含 JD 的一部分。只要职位描述看起来不完整（顶部或底部被截断、
  出现滚动条、条目编号不是从 1 开始、最后一句没写完），
  就把 partial_description 设为 true。
- 不要提取截图中的其他职位卡片、广告或推荐位。

## 输出
严格按结构化 schema 输出。看不到的字段返回 null。
"""


def build_text_user_prompt(text: str) -> str:
    return (
        "下面是用户从招聘网站复制的原始文本，请提取其中的职位信息。\n\n"
        "----- 复制的内容开始 -----\n"
        f"{text}\n"
        "----- 复制的内容结束 -----"
    )


VISION_USER_PROMPT = "请提取这张招聘页面截图中可见的职位信息。"
