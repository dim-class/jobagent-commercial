{
  "name": "jobagent-boss-detector",
  "version": "0.1.0",
  "private": true,
  "description": "Manifest V3 extension that reads the BOSS page the user already has open and hands the fields to the local JobAgent backend.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "test": "node --test tests/"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
